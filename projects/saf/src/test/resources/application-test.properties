# Properties define programmatically by DynamicBrokerInitializer to avoid test run conflict. Used ports / paths
saf.broker-url=nio://localhost:${dynamicBrokerPort}
saf.kaha-db-path=${dynamicKahaDbPath}